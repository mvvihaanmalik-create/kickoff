{
  "name": "kickoff",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ext": "vite build --config vite.config.ext.js && node scripts/copy-demo-bundle.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack:ext": "npm run build:ext && node scripts/pack-extension.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^6.0.7",
    "vitest": "^4.1.10"
  }
}
